plugins {
    id 'base'
}

// Root project is an aggregator for all subprojects.
// Each loader-specific subproject has its own build.gradle.

allprojects {
    apply plugin: 'base'

    group = mod_group_id
    version = mod_version

    repositories {
        mavenLocal()
    }
}